---
marker: "Use the `ck` skill instead"
---
Never use `grep`, `rg`, or `ripgrep` to search any file in the repository. Never use `awk` or `sed` to search files by pattern. Use the `ck` skill instead. `awk` and `sed` are permitted for text transformation, replacement, and positional extraction (e.g. line ranges).
